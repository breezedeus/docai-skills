repos:
  - repo: local
    hooks:
      - id: black-check
        name: black check
        entry: uv run black --check skills/ tests/
        language: system
        pass_filenames: false
        stages: [pre-commit]